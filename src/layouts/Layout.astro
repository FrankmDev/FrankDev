---

---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Astro description" />
    <meta name="viewport" content="initial-scale=1, width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>FrankDev</title>
  </head>
  <body class="flex items-center justify-center flex-col">
    <slot />
  </body>
</html>
<style is:global>
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }
  html {
    font-family: "Poppins", sans-serif;
    color: #f9f9f9;
    width: 100%;
    height: 100%;

    background: #000000;
    --gap: 3em;
    --line: 1px;
    --color: rgba(48, 48, 48, 0.2);

    background-image: linear-gradient(
        -90deg,
        transparent calc(var(--gap) - var(--line)),
        var(--color) calc(var(--gap) - var(--line) + 1px),
        var(--color) var(--gap)
      ),
      linear-gradient(
        0deg,
        transparent calc(var(--gap) - var(--line)),
        var(--color) calc(var(--gap) - var(--line) + 1px),
        var(--color) var(--gap)
      );
    background-size: var(--gap) var(--gap);
  }
  @font-face {
    font-family: "Poppins";
    src: url("/Poppins/Poppins-SemiBold.ttf");
    font-weight: normal;
    font-style: normal;
    font-display: swap;
  }
  body {
    letter-spacing: 0.1rem;
    gap: 2.2rem;
  }
  button {
    cursor: pointer;
  }
  .section {
    width: 83.33%;
    height: 100vh;
    max-width: 1500px;
    max-height: 1000px;
    background-color: #040404;
  }
  .container {
    -webkit-box-shadow: 0px 0px 30px -10px rgba(83, 47, 92, 1);
    -moz-box-shadow: 0px 0px 30px -10px rgba(83, 47, 92, 1);
    box-shadow: 0px 0px 30px -10px rgb(83, 47, 92);
  }
  .section-primary {
    font-size: 3rem;
    margin-top: 1.25rem;
  }

  /* HIGHLIGHT */

  .highlight {
    color: #d0c4e2;
    animation: lights 5.5s 750ms linear infinite;
  }
  @keyframes lights {
    0% {
      text-shadow:
        0 0 0.75em rgba(90, 47, 101, 0.15),
        0 0 0.1em rgba(75, 80, 146, 0.15),
        -0.75em -0.1em 0.5em rgba(209, 92, 106, 0),
        0.75em 0.1em 0.5em rgba(73, 129, 123, 0);
    }

    30% {
      text-shadow:
        0 0 0.75em rgba(90, 47, 101, 0.2),
        0 0 0.1em rgba(75, 80, 146, 0.2),
        -0.375em -0.1em 0.25em rgba(209, 92, 106, 0.1),
        0.375em 0.1em 0.25em rgba(73, 129, 123, 0.15);
    }

    40% {
      text-shadow:
        0 0 0.75em rgba(90, 47, 101, 0.2),
        0 0 0.1em rgba(75, 80, 146, 0.2),
        -0.1875em -0.1em 0.125em rgba(209, 92, 106, 0.1),
        0.1875em 0.1em 0.125em rgba(73, 129, 123, 0.15);
    }

    70% {
      text-shadow:
        0 0 0.75em rgba(90, 47, 101, 0.2),
        0 0 0.1em rgba(75, 80, 146, 0.2),
        0.375em -0.1em 0.25em rgba(209, 92, 106, 0.1),
        -0.375em 0.1em 0.25em rgba(73, 129, 123, 0.15);
    }

    100% {
      text-shadow:
        0 0 0.75em rgba(35, 16, 40, 0.15),
        0 0 0.1em rgba(18, 0, 50, 0.15),
        0.75em -0.1em 0.5em rgba(209, 92, 106, 0),
        -0.75em 0.1em 0.5em rgba(0, 33, 49, 0.1);
    }
  }
</style>
